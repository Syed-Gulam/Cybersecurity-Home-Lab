# Example saved search to detect abnormal new service creation
# Place this in a savedsearches.conf style file if importing to Splunk
[index="wineventlog"]
search = EventCode=7045 | stats count by ServiceName, ComputerName, User
